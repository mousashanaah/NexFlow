<div class="wrap" style="padding:50px 0 80px;max-width:820px">
  <h1 class="display" style="font-size:clamp(2rem,4vw,3rem);margin-bottom:20px">{{ page.title }}</h1>
  <div class="rte" style="line-height:1.7;color:var(--ink-soft)">{{ page.content }}</div>
</div>
{% schema %}{ "name": "Page" }{% endschema %}
